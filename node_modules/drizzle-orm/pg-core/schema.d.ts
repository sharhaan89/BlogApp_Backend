import { entityKind } from "drizzle-orm/entity.js";
import { SQL, type SQLWrapper } from "drizzle-orm/sql/sql.js";
import type { pgEnum } from "drizzle-orm/pg-core/columns/enum.js";
import { type pgSequence } from "drizzle-orm/pg-core/sequence.js";
import { type PgTableFn } from "drizzle-orm/pg-core/table.js";
import { type pgMaterializedView, type pgView } from "drizzle-orm/pg-core/view.js";
export declare class PgSchema<TName extends string = string> implements SQLWrapper {
    readonly schemaName: TName;
    static readonly [entityKind]: string;
    constructor(schemaName: TName);
    table: PgTableFn<TName>;
    view: typeof pgView;
    materializedView: typeof pgMaterializedView;
    enum: typeof pgEnum;
    sequence: typeof pgSequence;
    getSQL(): SQL;
    shouldOmitSQLParens(): boolean;
}
export declare function isPgSchema(obj: unknown): obj is PgSchema;
export declare function pgSchema<T extends string>(name: T): PgSchema<T>;
