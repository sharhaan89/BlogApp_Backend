import type { Column } from "drizzle-orm/column.js";
import { entityKind } from "drizzle-orm/entity.js";
import type { Casing } from "drizzle-orm/utils.js";
export declare function toSnakeCase(input: string): string;
export declare function toCamelCase(input: string): string;
export declare class CasingCache {
    static readonly [entityKind]: string;
    private cachedTables;
    private convert;
    constructor(casing?: Casing);
    getColumnCasing(column: Column): string;
    private cacheTable;
    clearCache(): void;
}
